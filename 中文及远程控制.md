#中文
> $sudo apt-get install ttf-wqy-zenhei

#中文输入法
> $sudo apt-get install scim-pinyin
> $sudo raspi-config

#SSH
> - $service ssh status
> - $service ssh start

#xrdp
> - $sudo apt-get install xrdp
> - 与Windows"远程桌面连接"链接